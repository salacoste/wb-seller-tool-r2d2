/** @type {import('next').NextConfig} */
const nextConfig = {
  // distDir: 'build',
  reactStrictMode: true,
  swcMinify: true,

  // experimental: {
  //   appDir: true,
  // },
}

module.exports = nextConfig
