import { NextPageContext } from 'next';

import { signIn } from 'next-auth/react';
import { authOptions } from '../api/auth/[...nextauth]';

export const S = async (props: any) => {
  return <p>sdsds</p>;
};

export default S;
